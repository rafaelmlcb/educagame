# Server
quarkus.http.port=8080
quarkus.http.host=0.0.0.0

# REST path (optional; default /)
# quarkus.http.root-path=/api

# WebSocket path is relative to root: ws://host:port/game

# CORS - whitelist (production: set to your frontend origin)
quarkus.http.cors=true
quarkus.http.cors.origins=${CORS_ORIGINS:http://localhost:5173,http://127.0.0.1:5173}
quarkus.http.cors.methods=GET, POST, OPTIONS
quarkus.http.cors.headers=accept, content-type, authorization
quarkus.http.cors.exposed-headers=
quarkus.http.cors.max-age=86400

# Security headers (OWASP)
quarkus.http.header.X-Frame-Options=DENY
quarkus.http.header.X-Content-Type-Options=nosniff
quarkus.http.header.X-XSS-Protection=1; mode=block
quarkus.http.header.Strict-Transport-Security=max-age=31536000; includeSubDomains; preload
quarkus.http.header.Content-Security-Policy=default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; connect-src 'self' ws: wss:; img-src 'self' data:

# Logging
quarkus.log.level=INFO
quarkus.log.category."com.educagame".level=DEBUG
